services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: behavioral_auth
      POSTGRES_USER: dev
      POSTGRES_PASSWORD: dev
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
volumes:
  postgres_data:
